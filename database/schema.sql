-- Canonical schema (mirrors the latest migration).
-- Run migrations/001_initial.sql on a fresh database to set up.
-- Use Alembic (backend/) for incremental schema management.
\i migrations/001_initial.sql
